apiVersion: v1
kind: Service
metadata:
  name: ingress
  namespace: cudn-demo
  annotations:
    metallb.io/address-pool: ip-addresspool-sno
spec:
  type: LoadBalancer
  ports:
  - port: 8080
    targetPort: 8080
